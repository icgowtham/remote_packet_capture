# remote_packet_capture
REST API based application to start packet capture on remote nodes.

Python based application which contains a server (master) and several agents (where agent application will be running). Using the server, packet capture can be triggered on the remote node.
